spring.application.name=pethealth

server.port=8080

spring.mongodb.uri=${MONGODB_URI}
spring.mongodb.database=${MONGODB_DATABASE:pethealth}

aws.s3.bucket=${AWS_S3_BUCKET:inha-capstone-03}
aws.s3.region=${AWS_REGION:us-east-1}

aws.bedrock.region=${AWS_BEDROCK_REGION:us-east-1}
aws.bedrock.model-id=${BEDROCK_MODEL_ID:amazon.nova-lite-v1:0}